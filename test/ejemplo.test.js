const assert = require('assert');
describe('Pruebas de ejemplo', () => {
 it('debería retornar 2', () => {
        assert.equal(1 + 1, 2);
    });
 it('Debería retornar 9', () => {
        assert.equal(3 * 3, 9);
    });
});